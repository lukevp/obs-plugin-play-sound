## OBS Setup

Enable websocket server in OBS, and copy the password for the server

## Usage

Install nodejs

Run `npm install` to restore packages

Run `node index.js password` (where password is the password to your websocket server)

